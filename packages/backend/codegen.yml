overwrite: true
schema: './src/graphql'
documents: null
generates:
  ./src/graphql/generated/index.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      allowEnumStringTypes: true
hooks:
  afterOneFileWrite:
    - prettier --write
