overwrite: true
schema: './src/graphql'
documents: null
generates:
  ./src/graphql/generated/index.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
hooks:
  afterOneFileWrite:
    - prettier --write
